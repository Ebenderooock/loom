# Binaries
/loom
/dist/
/bin/
*.exe
*.test
*.out

# Go
/vendor/
*.prof

# Node / frontend
web/node_modules/
web/dist/
web/.vite/
web/coverage/

# Node / frontend (website)
website/node_modules/
website/dist/
website/.vite/
website/coverage/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Local config & data
/config/
/data/
*.db
*.db-journal
*.sqlite
*.sqlite-journal
.env
.env.local

# Generated
/api/generated/
/web/src/api/generated/

# Logs
*.log
web/storybook-static
web/playwright-report/
web/test-results/
web/*.tsbuildinfo
run/data/
website/node_modules/
website/dist/

# Local MCP config — contains a live API key, keep local-only
/.mcp.json

# All node_modules (incl. MCP helper servers)
**/node_modules/

# Vendored / reference clones — do not commit (licensing)
/22/
/radarr/
/references/

# Local-only helm overrides (real ingress host, etc.)
deploy/helm/values-homelab.local.yaml

# Build-time secrets baked into the Docker image (TVDB key, etc.)
deploy/.build-secrets.env
