// Package prowlarrv1 provides a Prowlarr v1 API compatibility shim.
//
// It translates Prowlarr v1 API calls into Loom's internal indexer
// service so external tools (other *arr apps, Requestrr, etc.) can
// talk to Loom as though it were a Prowlarr instance.
package prowlarrv1
